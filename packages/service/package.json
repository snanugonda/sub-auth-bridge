{
  "name": "@open-ai-sub-auth/service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "HTTP API that routes chat requests to the ChatGPT Codex backend using subscription OAuth instead of API-key billing.",
  "scripts": {
    "build": "tsc -p .",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.6.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  }
}
